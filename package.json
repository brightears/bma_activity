{
  "name": "bma-activity-report",
  "version": "2.0.0",
  "description": "BMA Activity Report System with Supabase",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "cd client && npm run build",
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "supabase:link": "supabase link --project-ref YOUR_PROJECT_REF",
    "supabase:push": "supabase db push"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "vercel": "^32.0.0",
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}